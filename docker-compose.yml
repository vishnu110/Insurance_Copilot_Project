services:

  insurance-bot:

    build: .

    container_name: insurance-bot

    ports:

      - "8000:8000"
      - "8501:8501"

    env_file:

      - .env

    restart: unless-stopped